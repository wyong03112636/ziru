<!--pages/detail/detail.wxml-->
<block wx:if="{{result}}">
  <banner result="{{result}}"></banner>
  <serve  result="{{result}}"></serve>
  <traffic result="{{result}}"></traffic>
  <question result="{{result}}"></question>
</block>

